<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="search"
        android:icon="@drawable/ic_baseline_"
        android:id="@+id/search"
        app:showAsAction="ifRoom"/>

    <item android:title="refresh"
        android:icon="@drawable/ic_refresh_"
        android:id="@+id/refresh"
        app:showAsAction="ifRoom"/>

    <item android:title="menuvertical"
        android:icon="@drawable/ic_more_vert"
        android:id="@+id/vertical"

        app:showAsAction="ifRoom">
    <menu>

        <item android:id="@+id/shareapp"
            android:title="Share app"/>

        <item android:id="@+id/rateapp"
            android:title="Rate app"/>

        <item android:id="@+id/feedback"
            android:title="Feedback"/>

        <item android:id="@+id/policy"
            android:title="Privacy Poicy"/>

        <item android:id="@+id/setting"
            android:title="Settings"/>


    </menu>
</item>

</menu>